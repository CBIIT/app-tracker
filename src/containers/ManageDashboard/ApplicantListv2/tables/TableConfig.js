import { getColumnSearchProps } from '../../Util/ColumnSearchProps';
import { transformDateTimeToDisplay } from '../../../../components/Util/Date/Date';
// TODO: import { } from '@ant-design/icons';
import { OWM_TEAM } from '../../../../constants/Roles';

export const getApplicantColumns = ({
	searchText,
	// TODO: Other parameters
	onSendRejectionClick,
}) => {
	const baseColumn = [
		/* TODO: Define columns array */
	];
	return baseColumns;
};

export const getSplitApplicantColumns = (params) => {
	// TODO: Return columns for split table view
	// Might be same as getApplicantColumns or slightly different
};
