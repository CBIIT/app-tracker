import SectionHeader from './SectionHeader/SectionHeader';

const finalizeVacancy = () => {
	return (
		<>
			<SectionHeader title='Basic Vacancy Information' />
			<div className='SectionContent'>
				<h2>Position Title</h2>
			</div>
		</>
	);
};

export default finalizeVacancy;
